
import 'package:flutter/material.dart';

class AddLogScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Add New Log')),
      body: Center(
        child: Text('Form to add new log goes here'),
      ),
    );
  }
}
